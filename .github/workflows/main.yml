name: Keep Alive - Travel Agent

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -f https://travelagent-bqtmr6s69hxtqk8oxibuqj.streamlit.app/ || echo "Ping failed"
